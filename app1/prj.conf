# An ordinary application config. Everything that makes this manageable by the
# runtime comes from the runtt snippet, not from here.
CONFIG_LOG=y
CONFIG_LOG_MODE_IMMEDIATE=y
CONFIG_MAIN_STACK_SIZE=2048
